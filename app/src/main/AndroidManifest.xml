<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.edumotter.oscar">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="The Oscar App"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".utils.Session"
        android:theme="@style/Theme.Oscar">
        <activity android:name=".activities.VotedActivity" />
        <activity android:name=".activities.ConfirmVoteActivity" />
        <activity android:name=".activities.DetailedFilmActivity" />
        <activity android:name=".activities.DirectorsActivity" />
        <activity android:name=".activities.FilmsActivity" />
        <activity android:name=".activities.DashboardActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>